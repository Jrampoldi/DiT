# Diffusion Models
This repository contains implementations of DiT (Scalable Diffusion Models with Transformers) with a docker image

## Pull Docker Image
~~~bash
docker pull rampoldijourdan/dit:v1.0
~~~

## Run test with container
~~~bash
docker run --gpus all -it rampoldijourdan/dit:v1.0
./run.sh
~~~
